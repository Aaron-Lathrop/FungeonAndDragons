// the publicPath is the path that will be prefaced in the dist folder
// when referencing resources. Normally for GitHub Pages the public
// path should be "/<your-app-name>/" but since this project is using
// a CNAME, that path does not exist and just a "/" should be used
module.exports = {
	publicPath: "/",
};
